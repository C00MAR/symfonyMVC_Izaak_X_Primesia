{% extends 'base.html.twig' %}

{% block title %}Distribution de points{% endblock %}

{% block body %}
    <h1>Distribution de points</h1>
    
    <p>Utilisez cette fonction pour distribuer 1000 points à tous les utilisateurs actifs.</p>
    
    <form method="post" action="{{ path('app_admin_points_distribute') }}">
        <input type="hidden" name="_token" value="{{ csrf_token('distribute_points') }}">
        <button type="submit" onclick="return confirm('Êtes-vous sûr de vouloir distribuer 1000 points à tous les utilisateurs actifs ?')">
            Distribuer 1000 points à tous les utilisateurs actifs
        </button>
    </form>
    
    <p><small>Note: Cette action sera traitée en arrière-plan via Messenger.</small></p>
{% endblock %}
